# Plain secrets file (alternative to 1Password)
#
# IMPORTANT: Copy this file to secrets.env and fill in real values
#   cp secrets.env.example secrets.env
#
# WARNING: Never commit secrets.env to git!
#   echo "deploy/secrets.env" >> .gitignore

# Server sudo password for deployer user
DEPLOYER_SUDO_PASS=your-sudo-password-here

# Database password (remove this line if using SQLite)
DEPLOYER_DB_PASSWORD=your-database-password-here

# Laravel APP_KEY (generate with: php artisan key:generate --show)
DEPLOYER_APP_KEY=base64:your-app-key-here

# Optional secrets (add more as needed)
# DEPLOYER_MAIL_PASSWORD=your-mail-password
# DEPLOYER_STRIPE_KEY=sk_live_xxxxx
